{ok, List} = file:list_dir("apps"),
Deps = [{list_to_atom(Dep), {local, "apps/" ++ Dep}} || Dep, filelib:is_dir(Dep) <- List}],
[
    {deps, Deps},
    {plugins, [
        {rebar_subdir_plugin, {git, "https://github.com/sjanota/rebar_subdir_plugin.git", {branch, "master"}}}
    ]}
].